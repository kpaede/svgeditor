export interface SvgEditorSettings {
	defaultWidth: number;
	defaultHeight: number;
	showInlineEditButton: boolean;
}

export const DEFAULT_SETTINGS: SvgEditorSettings = {
	defaultWidth: 800,
	defaultHeight: 600,
	showInlineEditButton: true
};
